The purpose of this file is to check if the frontend coding file works as expected.

It contains
1.  `ui/`
-   Purpose : Check if `ui/` works as expected.
2.  `index.tsx`
-   Purpose : Import coding testing function from different files in `app/page.tsx`
